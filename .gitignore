# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Environments
.env
.venv/
env/
venv/

# Database files
# backend/*.db

# ABS DataPack zips and extracted sample CSVs — large files, never commit
data/datapacks/
data/vic_sample/

# GTFS feeds — large downloads, never commit
data/gtfs/

# Overture Maps places parquet + ABS building approvals zips — large downloads, never commit
data/overture/
data/building_approvals/

# Claude Code machine-local permission cache — not shareable project config
.claude/settings.local.json

# SQLite dev database — local only
backend/*.db

# Node modules
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

backend/.venv/

__pycache__/
*.pyc
*.pyo
.pytest_cache/
