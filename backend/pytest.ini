[pytest]
testpaths = tests
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
markers =
    unit: pure-function / module-level tests
    integration: route or DB tests
asyncio_mode = auto
